pe# Credenciales para conectar a PostgreSQL:
# - Host: localhost
# - Puerto: 5432 (puerto por defecto de PostgreSQL)
# - Base de datos: chrispar
# - Usuario: postgres
# - Contraseña: [TU_CONTRASEÑA]
#
# IMPORTANTE: Crear la base de datos antes de ejecutar:
# psql -U postgres -c "CREATE DATABASE chrispar;"

DATABASE_URL=postgresql://postgres:TU_CONTRASEÑA@localhost:5432/chrispar

# Mirror DB (Postgres en contenedor separado - OPCIONAL):
# Descomentar solo si usas modo externo con Docker (dos contenedores)
# MIRROR_DATABASE_URL=postgresql://postgres:TU_CONTRASEÑA@localhost:5433/chrispar

# Clave secreta para JWT y sesiones
# CAMBIAR por una clave segura en producción
SECRET_KEY=tu_clave_secreta_aqui

# Configuración Flask
FLASK_APP=app.py
FLASK_DEBUG=0
